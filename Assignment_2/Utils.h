#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <cmath>
#include <time.h>
#include <fstream>

using namespace std;

void printPrimes(bool* list, unsigned long long size);

long countPrimes(bool* list, long long size);

#endif